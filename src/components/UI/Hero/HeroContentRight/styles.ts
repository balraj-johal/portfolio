import { motion } from "framer-motion";
import { styled } from "@phntms/css-components";

import css from "./style.module.css";

export const HeroContentRightParagraph = styled(motion.p, {
  css: css.HeroContentRightParagraph,
});
